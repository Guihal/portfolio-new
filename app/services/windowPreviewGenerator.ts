// TODO: P8-06 — html-to-image обёртка для генерации превью окон в taskbar.
// Извлекается из stores/frame.ts:52-94 (MutationObserver в store, см. RULES.md §5).
// DOM-доступ под guard `if (import.meta.client)` (см. RULES.md §2a).

export const TODO_WINDOW_PREVIEW_GENERATOR = true as const;
