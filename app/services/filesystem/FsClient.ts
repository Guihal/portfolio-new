// TODO: P8-03 — FsClient: get/list/getBreadcrumbs обёртки над /api/filesystem/*.
// Контракт: $fetch (SSR-safe), zod-валидация ответа, без module-scope state.
// См. docs/RULES.md §2a (SSR-контракт services/).

export const TODO_FS_CLIENT = true as const;
