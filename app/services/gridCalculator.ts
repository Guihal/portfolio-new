// TODO: P8-08 — pure cell-grid calc (rows/cols → cell rects).
// Извлекается из useGridCells.ts:63-77 (pure compute в composable, см. RULES.md §5).

export const TODO_GRID_CALCULATOR = true as const;
