// TODO: P8-07 — pure positioning calc для taskbar tooltip.
// Извлекается из useTaskbarTooltips.ts:60-73 (DOM API в composable, см. RULES.md §5).
// Pure: in = anchor rect + tooltip size + viewport, out = {x, y, placement}.

export const TODO_TOOLTIP_STATE = true as const;
